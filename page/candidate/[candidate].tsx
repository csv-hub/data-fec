
// function useDatabaseQuery(...args: any[]) {}

export default function Candidate({ candidate }: { candidate: string }) {
    // const { loading, result, query } = useDatabaseQuery((query) => query.withEqual('candidate', candidate))

    return (
        <div>This is a candidate</div>
    )
}